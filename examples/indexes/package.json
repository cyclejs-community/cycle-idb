{
  "name": "indexes",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prebrowserify": "rm -rf ./dist && mkdir dist",
    "browserify": "browserify main.js -t babelify --outfile dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@cycle/dom": "^17.6.0",
    "@cycle/run": "^3.1.0",
    "cycle-idb": ">=0.0.6",
    "xstream": "^10.8.0"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "babel-register": "^6.24.1",
    "babelify": "^7.3.0",
    "browserify": "^14.4.0"
  }
}
